# 环境变量配置示例
# 复制此文件为 .env 并根据实际情况修改

# ============ Embedding模型配置 ============
# 模型名称（首次运行会自动下载）
EMBEDDING_MODEL=BAAI/bge-m3
# 运行设备: cpu 或 cuda
EMBEDDING_DEVICE=cpu

# ============ ChromaDB配置 ============
# 向量数据库存储路径
CHROMA_PATH=./data/vector_db/chroma_kb
# 集合名称
CHROMA_COLLECTION_NAME=test_knowledge

# ============ LLM配置（可选，用于AI生成测试用例） ============
# OpenAI API Key（如果不配置，系统将使用简单模板模式）
LLM_API_KEY=
# API基础URL（可替换为国内镜像或其他兼容接口）
LLM_BASE_URL=https://api.openai.com/v1
# 模型名称
LLM_MODEL=gpt-3.5-turbo

# ============ JIRA配置 ============
# JIRA地址（Atlassian Cloud示例：https://your-company.atlassian.net）
JIRA_URL=https://your-company.atlassian.net
# 邮箱账号
JIRA_EMAIL=your-email@company.com
# API Token（在 https://id.atlassian.com/manage-profile/security/api-tokens 创建）
JIRA_API_TOKEN=
# 项目Key（可选，为空则拉取所有项目，如：TEST、DEV）
JIRA_PROJECT_KEY=

# ============ Confluence配置 ============
# Confluence地址（Atlassian Cloud示例：https://your-company.atlassian.net/wiki）
CONFLUENCE_URL=https://your-company.atlassian.net/wiki
# 邮箱账号（可与JIRA相同）
CONFLUENCE_EMAIL=your-email@company.com
# API Token（可与JIRA相同）
CONFLUENCE_API_TOKEN=

# ============ API服务配置 ============
API_HOST=0.0.0.0
API_PORT=8000

# ============ 定时同步配置 ============
# 同步间隔（小时），默认6小时
SYNC_INTERVAL_HOURS=6

# ============ HF镜像（国内加速） ============
HF_ENDPOINT=https://hf-mirror.com
