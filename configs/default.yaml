# 默认配置
embedding:
  model: "BAAI/bge-m3"
  device: "cpu"

vector_store:
  path: "./data/vector_db/chroma_kb"
  collection_name: "test_knowledge"

api:
  host: "0.0.0.0"
  port: 8000

llm:
  # 可选：配置LLM API Key
  # api_key: "sk-xxx"
  # base_url: "https://api.openai.com/v1"
  # model: "gpt-3.5-turbo"
