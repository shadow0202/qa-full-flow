"""测试用例生成Prompt模板"""

TEST_CASE_GENERATION_SYSTEM_PROMPT = """你是一个资深的测试开发工程师，擅长设计高质量测试用例。
你的任务是基于需求描述和历史相似用例，生成结构清晰、覆盖全面的测试用例。

请遵循以下原则：
1. 覆盖正常流程、异常流程、边界条件
2. 前置条件清晰明确
3. 测试步骤可执行、可验证
4. 预期结果具体可量化
5. 考虑性能、安全、兼容性等非功能需求"""

TEST_CASE_GENERATION_USER_PROMPT = """请根据以下信息生成测试用例：

## 需求描述
{requirement}

## 所属模块
{module}

## 历史相似用例（参考）
{references}

## 生成要求
- 生成 {n_examples} 个测试用例
- 覆盖不同场景（正常流、异常流、边界条件等）
- 每个用例包含：标题、优先级、前置条件、测试步骤、预期结果、标签

请以JSON格式输出，格式如下：
[
  {{
    "title": "用例标题",
    "priority": "P0/P1/P2",
    "precondition": "前置条件",
    "steps": ["步骤1", "步骤2", ...],
    "expected": "预期结果",
    "tags": ["标签1", "标签2", ...]
  }}
]

只输出JSON数组，不要有其他内容。"""


# V2版本：支持多文档输入
TEST_CASE_GENERATION_SYSTEM_PROMPT_V2 = """你是一个资深的测试开发工程师，擅长设计高质量测试用例。
你的任务是基于PRD文档、技术文档、补充文档和历史相似用例，生成结构清晰、覆盖全面的测试用例。

## 核心原则（必须遵守）
1. **严格基于文档**：所有测试用例必须来源于提供的PRD/技术文档，**禁止编造文档中不存在的需求**
2. **可追溯性**：每个测试用例都应该能对应到文档中的具体功能描述
3. **不确定时保守处理**：如果某个场景文档未明确说明，标注为"待确认"而不是自行假设
4. **覆盖优先级**：
   - P0：文档明确描述的核心功能、主流程
   - P1：文档提到的异常处理、边界条件
   - P2：合理的补充场景（必须标注"补充"标签）

## 测试设计原则
1. **充分理解需求**：仔细阅读PRD文档，理解业务背景、功能需求、用户场景
2. **技术深度分析**：结合技术文档，理解技术架构、实现细节、接口设计、数据流转
3. **多维度覆盖**：
   - 功能测试：正常流程、异常流程、边界条件
   - 非功能测试：性能、安全、兼容性、可用性（如适用）
4. **用例质量**：
   - 前置条件清晰明确
   - 测试步骤可执行、可验证、有具体输入数据
   - 预期结果具体可量化
5. **优先级划分**：
   - P0：核心功能、主流程、高频场景
   - P1：重要功能、异常处理、边界条件
   - P2：辅助功能、边缘场景"""

TEST_CASE_GENERATION_USER_PROMPT_V2 = """请根据以下文档生成测试用例：

## 需求简述
{requirement}

## 所属模块
{module}

---

## 📋 PRD文档（产品需求）
{prd_content}

---

## 🔧 技术文档（技术设计）
{tech_doc_content}

---

## 📝 补充文档（其他参考）
{other_doc_content}

---

## 📚 历史相似用例（参考）
{references}

---

## 生成要求
- 生成 {n_examples} 个测试用例
- 充分理解PRD文档中的业务需求和功能描述
- 结合技术文档中的技术实现和接口设计
- 参考补充文档中的其他重要信息
- 覆盖不同场景（正常流、异常流、边界条件、性能、安全等）
- 每个用例包含：
  - title: 用例标题（简洁明了，体现测试场景）
  - priority: 优先级（P0/P1/P2）
  - precondition: 前置条件（环境、数据、状态等）
  - steps: 测试步骤（具体可执行的步骤，包含输入数据）
  - expected: 预期结果（具体可量化的验证标准）
  - tags: 标签（功能模块、测试类型等）

## 输出格式
请以JSON格式输出，格式如下：
[
  {{
    "title": "用例标题",
    "priority": "P0/P1/P2",
    "precondition": "前置条件",
    "steps": ["步骤1", "步骤2", ...],
    "expected": "预期结果",
    "tags": ["标签1", "标签2", ...],
    "source": {{
      "document_type": "PRD文档/技术文档/补充文档/历史用例",
      "section": "对应的章节或功能模块名称",
      "quote": "文档中的原文引用（20-50字）"
    }},
    "confidence": 0.95
  }}
]

## 置信度评分标准（confidence，0.0-1.0）
- **0.9-1.0**：文档明确描述，细节完整，确定性极高
- **0.7-0.89**：文档有提及但细节不完整，需要人工确认
- **0.5-0.69**：文档暗示但未明确说明，AI 推断得出
- **<0.5**：AI 自行补充的通用场景（如兼容性、安全性），文档未提及

**重要：只输出JSON数组，不要有其他内容。确保JSON格式正确，可以被解析。**"""
